import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder: app requires API service, cannot be tested in isolation
    expect(true, isTrue);
  });
}
